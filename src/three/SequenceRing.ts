// In the piano-tiles version, the sequence display is rendered as a React
// overlay rather than 3D geometry. This module is reserved for a future
// 3D ring display mode.

export class SequenceRing {
  dispose(): void {
    // no-op
  }
}
